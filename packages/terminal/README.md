# terminal

Not implemented yet.

xterm.js-based terminal (SPEC section 13). Local mode: Worker + WASM shell. Cloud mode: WebSocket to a PTY in a container. Never fakes shell output.

See the root README's Roadmap section and SPEC.md for the full spec this will be built against.
